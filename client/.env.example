# API Configuration
# For local development (default server port is 5000)
VITE_API_URL=http://localhost:5000/api

# For production deployment (replace with your actual Render backend URL)
# VITE_API_URL=https://your-backend-name.onrender.com/api

# Note: The /api suffix is important as all routes are prefixed with /api
